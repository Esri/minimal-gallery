import { jsxFactory } from "esri/widgets/support/widget";

export const MapPanel = (props) => ({
    render: () => <div id="map-panel" />
});

export const ScenePanel = (props) => ({
    render: () => <div id="scene-panel" />
});

export const AppPanel = (props) => ({
    render: () => <div id="app-panel" />
});
