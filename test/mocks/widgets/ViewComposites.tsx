import { jsxFactory } from "esri/widgets/support/widget";

export const WebMap = () => ({ render: () => <div id="webmap" /> });

export const WebScene = () => ({ render: () => <div id="webscene" /> });
